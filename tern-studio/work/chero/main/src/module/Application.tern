

annotation Module(name: String);
annotation ManagedOperation(name: String, value: String);
annotation ManagedResource(operation: ManagedOperation);

@Module("foo")
class Service {
   
   get(@Param("name") name) {
   }
}