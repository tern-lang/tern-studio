import io.BlahBlah;
import org.simpleframework.http.Request;
class Max {
   static max(a,b){  
      return a > b ? a : b;
   }
   static max(a,b,c){
      if(a > b) {
         return max(a, c);
      }
      return max(b, c);
   }
}
class Blah{
   test(val){
      new BlahBlah();
      System.err.println("The import fucked up!!");
   }
}
