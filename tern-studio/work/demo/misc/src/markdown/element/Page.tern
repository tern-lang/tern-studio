
enum Page {
   HOME("Home", "/"),
   GUIDE("Guide", "/videos"),
   DOWNLOAD("Download", "/download");
   
   const title;
   const path;
   
   new(title, path) {
      this.title = title;
      this.path = path;
   }
}