
func fib(): Iterable<BigInteger> {
   let a: BigInteger = 0;
   let b: BigInteger = 1;
   
   loop {
      yield b;
      (a, b) = (b, a + b);
   }
}
let x =
fib().iterator()
     .limit(100)
     .reverse()
     .gather()
     .stream()
     .findFirst()
     .get();
     
     println(x);