class Foo{
   test(n){
      if(n >0) {
         for(fn in this.class.functions){ 
            println(fn);
            if(fn.name == "test")
               fn(n-1);
         }
      }
   }
}
Foo().test(10);