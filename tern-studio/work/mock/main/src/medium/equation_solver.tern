

func convertToMultiple(top, bottom, result) {
   
}

enum Operation {
   MUL,
   DIV
}

class Calculation {
   let left;
   let right;
   let opl
   
   new(left, right, op) {
      this.left = left;
      this.right = right;
      this.op = op;
   }
}
