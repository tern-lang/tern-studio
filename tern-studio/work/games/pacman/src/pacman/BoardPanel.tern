import awt.Graphics;
import swing.JPanel;

class BoardPanel extends JPanel {

   private const b: Board;

   public new(b: Board) {
      this.b = b;
   }

   public paint(g: Graphics) {
      b.draw(g);
   }
}
