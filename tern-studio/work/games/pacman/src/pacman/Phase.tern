
enum Phase {
   MENU,
   START,
   STARTING,
   PLAY,
   PLAYING,
   GAME_OVER,
   WIN,
   DEMO;

   public isPaused(): Boolean {
      return this == MENU || this == WIN || this == DEMO || this == GAME_OVER;
   }
}
