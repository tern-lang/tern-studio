
import static lang.Integer.MAX_VALUE; 

import mario.core.GameFrame;
import mario.core.GamePanel;

func start(scale) {
   let g = new GameFrame(scale);
   
   try {
      g.start();
      sleep(MAX_VALUE);
   } catch(e) {
      e.printStackTrace();
   }
}

start(2);
  
     