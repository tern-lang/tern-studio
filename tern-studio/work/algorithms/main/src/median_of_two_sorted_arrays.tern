// https://leetcode.com/problems/median-of-two-sorted-arrays/

let median = findMedianTwoSortedArrays([1, 2], [3, 4]);

println(median);

func findMedianTwoSortedArrays(a, b){
   let length = a.length + b.length;
   let position = length / 2;
   let even = length %2 == 0;
   
   

}
